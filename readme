sudo apt-get update
sudo apt-get install lighttpd
sudo apt-get install python-webpy

# my lighttpd.conf not work, need to leverage original lighttpd configuration

sudo ln -s /vagrant/static /var/www/static

# http://localhost/static/loushu

# set proxy
export http_proxy=http://web-proxy.cce.hp.com:8080
export https_proxy=https://web-proxy.cce.hp.com:8080
